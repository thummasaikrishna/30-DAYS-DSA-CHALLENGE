class Solution {
    public int sum(int num1, int num2) {
        int[] Arr = new int[] {num1, num2};
        int[] ps = new int[Arr.length+1];
        for(int i=0;i<Arr.length;i++)
        {
            ps[i+1] = ps[i]+Arr[i];
        }
        return ps[Arr.length];
    }
}
