class Solution {
    public int missingNumber(int[] nums) {
        int size = nums.length;
        int sum = 0;
        for(int i=0;i<size;i++)
       {
            sum = sum + nums[i];
       }
       int res1 = (size*(size+1))/2;
        int res2 = res1-sum;
        return res2;
    }
}
